import React from "react";

function EmployeeDetail() {
  return (
  <div className="text-center">
    {/* {props.results.map(result => (
      <li className="list-group-item" key={result.id}>
        <img alt="Employee" className="img-fluid" src={props.result.picture.thumbnail} style={{ margin: "0 auto" }} />
        <h3>{props.result.name.first} {props.result.name.last}</h3>
        <h3>Call Office {props.results.phone}</h3>
        <h3>Call Mobile {props.results.cell}</h3>
        <h3>Email {props.results.email}</h3>
    </li>
    ))} */}
    </div>  
  );
}

export default EmployeeDetail;

import React from "react";
import ListGroup from 'react-bootstrap/ListGroup';

function EmployeeList() {
  return (
    <ListGroup>
        <ListGroup.Item>
            Hi
        </ListGroup.Item>
    </ListGroup>
  );
}

// now create template for employee avatar, name, title, etc

export default EmployeeList;
